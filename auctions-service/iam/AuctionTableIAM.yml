AuctionTableIAM:
  Effect: 'Allow'
  Action:
    - dynamodb:PutItem
    - dynamodb:Scan
    - dynamodb:GetItem
    - dynamodb:UpdateItem
  Resource: ${self:custom.AuctionTableDetails.arn}  ##Self will go to serverless.yml file